#include <stdio.h>

float main()
{
    int a=1, b=2;
    float c=3.3,d=4.4, sum1, sum2;
    sum2=c+d;
    sum1=a+b+c+d;
    printf("sum1 is %f      sum2 is %f", sum1, sum2);
    return 0;
}
