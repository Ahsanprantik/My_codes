#include <stdio.h>

int main()
{
    float x1, x2, a,b,c,m, n;
    scanf("%f %f %f", &a, &b, &c);
    n=m**2;
    n=(b**2)-4*a*c;
    x1=(-b+m)/(2*a);
    x2=(-b-m)/(2*a);
    printf("x1=%f x2=%f", x1, x2);
    }
