#include<bits/stdc++.h>
using namespace std;

int main(){
   int t, n, q, st, en, mn, i, j;

   scanf("%d", &t);

   for(i=1; i<=t; i++)   {
        scanf("%d %d", &n, &q);
        int ara[n];
        for(j=0; j<n; j++)  ara[j] = 20010;
        while(q--)  {
            scanf("%d %d %d", &st, &en, &mn);
            for(j=st-1; j<en; j++)  {
                if()
            }
        }
   }

   return 0;
}
