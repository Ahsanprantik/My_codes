#include <stdio.h>
#include <string.h>
/*UNSOLVED*/

int main()
{
	int n, q, t, k, d;
	scanf("%d %d", &n, &q);
	char serve[++n][1000001];
	memset(serve, 0, sizeof(serve))
	int ans[q];
	for(i=1; i<n; i++)	{
		scanf("%d %d %d", &t, &k, &d);
		for(j=t; d; d--, j++)	{
			if(serve[i][t]) break;
		}
		if(d)	ans[i]=
	}
}