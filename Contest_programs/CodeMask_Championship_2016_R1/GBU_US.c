#include <stdio.h>

int main()
{
	int t, i, n, j, c;
	char hs[4];
	scanf("%d", &t);
	int ans[t];
	for(i=0; i<t; i++) {
		scanf("%d", &n);
		for(j=0; j<n; j++) {
			scanf("%d", hs);
			if(c==0 && strcmp(hs, "Good")==0) break;
		}
		for( ; j=
		