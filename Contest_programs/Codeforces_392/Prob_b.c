#include <stdio.h>

#define if_f(x, y)	if(gar[i-j] == 'x')	clr[1][y] = 

int main()
{
	int i, clr[3][4];
	char gar[101];
	scanf("%s", gar);
	memset(clr, 0; sizeof(clr));
	clr[2][0] = 'R';
	clr[2][1] = 'B';
	clr[2][2] = 'G';
	clr[2][3] = 'Y';

	for(i=0; gar[i]; i++)	{
		if(gar[i] == '!' && i > 2)	{
			for(j = 3; j; j--)	{
				if_f(R, 0);
				if_f(B, 1);
				if_f(G, 2);
				if_f(Y, 3);
			}
			for(j = 0; clr[1][j]; ++j) ;
			clr[0][j]++;
			gar[i] = clr[2][j];
		}
		else {
			for(j=i; )
		}
	}

}